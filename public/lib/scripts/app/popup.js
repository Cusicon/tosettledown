$(document).ready(() => {
    
});

//-- This function holds all possible popUp in this application.
function popUpContents(){
    function welcomePage (html, image) {
        html = `html`
        return ;
    }
} popupContents();